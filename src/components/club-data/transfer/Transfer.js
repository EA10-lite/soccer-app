import React from 'react';
import './Transfer.css';

function Transfer({transfer}){
    console.log(transfer)
    return (
        <div className="transfer__container"></div>
    )
}

export default Transfer;